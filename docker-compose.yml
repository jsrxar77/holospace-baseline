services:
  # 1. Servidor de Aplicación HoloSpace Core Multi-Tenant
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: holospace_app
    restart: unless-stopped
    command: ["node", "--watch", "server.js"]
    environment:
      - NODE_ENV=development
      - PORT=3001
      - HS_PORT=3001
      - DATABASE_URL=postgres://${POSTGRES_USER:-holospace_admin}:${POSTGRES_PASSWORD:-Secr3tP@ssword2026}@postgres:5432/${POSTGRES_DB:-holospace_saas}
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=${JWT_SECRET:-holospace_super_secret_jwt_key_2026_x89f_aes}
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./public:/app/public
      - ./server.js:/app/server.js
      - ./lib:/app/lib
      - ./modules:/app/modules
      - ./package.json:/app/package.json
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - holospace_network

  # 2. Base de Datos Relacional PostgreSQL 16 con Row-Level Security (RLS)
  postgres:
    image: postgres:16-alpine
    container_name: holospace_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-holospace_admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-Secr3tP@ssword2026}
      POSTGRES_DB: ${POSTGRES_DB:-holospace_saas}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./data/init-schema.sql:/docker-entrypoint-initdb.d/01-init-schema.sql:ro
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-holospace_admin} -d ${POSTGRES_DB:-holospace_saas}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - holospace_network

  # 3. Redis para Caché de Entitlements, Sesiones y Rate-Limiting
  redis:
    image: redis:7-alpine
    container_name: holospace_redis
    restart: unless-stopped
    ports:
      - "6382:6379"
    volumes:
      - redis_data:/data
    networks:
      - holospace_network

  # 4. Proxy Reverso Nginx con Enrutamiento Multi-Tenant
  nginx:
    image: nginx:alpine
    container_name: holospace_proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - holospace_network

  # 5. Servicio de App Movil ScanBan Scanner (Expo Web & QR Metro Bundler)
  mobile:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: holospace_mobile
    restart: unless-stopped
    command: ["npx", "expo", "start", "--web", "--host", "lan", "--port", "8081"]
    ports:
      - "8081:8081"
      - "19000:19000"
      - "19001:19001"
    environment:
      - EXPO_NO_TELEMETRY=1
      # IP LAN del host Mac: permite que config.ts resuelva SERVER_URL correctamente
      # desde el browser externo y desde Expo Go en dispositivos fisicos.
      # Actualizar este valor si cambia la IP de la maquina en la red local.
      - EXPO_PUBLIC_SERVER_IP=192.168.100.247
      - REACT_NATIVE_PACKAGER_HOSTNAME=192.168.100.247
    volumes:
      - ./modules:/app/modules
      - ./web/expo-index.html:/app/public/index.html:ro
      - ./web:/app/web
      - ./metro.config.js:/app/metro.config.js
      - ./app.json:/app/app.json
    depends_on:
      - app
    networks:
      - holospace_network

  # 6. Servicio Automatizado de Backups Diarios
  backups:
    image: prodrigestivill/postgres-backup-local:16-alpine
    container_name: holospace_backups
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_DB=${POSTGRES_DB:-holospace_saas}
      - POSTGRES_USER=${POSTGRES_USER:-holospace_admin}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-Secr3tP@ssword2026}
      - SCHEDULE=@daily
      - BACKUP_KEEP_DAYS=30
      - BACKUP_KEEP_WEEKS=12
      - BACKUP_KEEP_MONTHS=12
      - POSTGRES_EXTRA_OPTS=-Z 9
    volumes:
      - ./backups:/backups
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - holospace_network

volumes:
  postgres_data:
  redis_data:

networks:
  holospace_network:
    driver: bridge
